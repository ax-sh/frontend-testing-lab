import type { Meta, StoryObj } from '@storybook/react'
import {{pascalCase targetDestinationFileName}} from './{{kebabCase targetDestinationFileName}}'

//function {{pascalCase targetDestinationFileName}}() {
//  return <div className="bg-red-500">Test {{pascalCase targetDestinationFileName}}</div>
//}

const meta = {
    title: 'UI/{{pascalCase targetDestinationFileName}}',
    component: {{pascalCase targetDestinationFileName}},
    // tags: ['autodocs'],

    } satisfies Meta<typeof {{pascalCase targetDestinationFileName}}>

        export default meta
        type Story = StoryObj<typeof meta>

        // More on writing stories with args: https://storybook.js.org/docs/writing-stories/args
        export const Default: Story = {
        args: {},
        parameters:{
        layout: 'fullscreen',
    }
}